require 'spec_helper'

describe Titular do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Titular.create!(@valid_attributes)
  end
end
