class MarcaRepresentante < ActiveRecord::Base
  belongs_to :marca

  # Crea una instancia con el tipo que se indica
  #def self.set_representante(params)
  #  MarcaRepresentante.create!(
  #    :tipo => params[:tipo], 
  #    :marca_id => params[:marca_id],
  #    :representante_id => params[:representante_id]
  #  )
  #end
end
