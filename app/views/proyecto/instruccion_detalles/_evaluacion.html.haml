= simple_form_for @instruccion_detalle, :html => {:method => :post}, :url => revisada_proyecto_proyecto_instruccion_instruccion_detall_path(@proyecto, @instruccion, @instruccion_detalle) do |evaluacion|
  = evaluacion.input :comentario_evaluacion
  = evaluacion.collection_radio :calificacion, [[10, 'Muy bueno'] ,[8, 'Bueno'],[6, 'Regular'],[4, 'Malo'],[2, 'Muy malo']], :first, :last

  = evaluacion.button :submit, 'Registrar evaluación'
