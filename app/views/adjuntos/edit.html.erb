<h1>Editing adjunto</h1>

<% form_for(@adjunto) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @adjunto %> |
<%= link_to 'Back', adjuntos_path %>